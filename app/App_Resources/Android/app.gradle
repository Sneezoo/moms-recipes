// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "de.sneezoo.MomsRecipes"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
